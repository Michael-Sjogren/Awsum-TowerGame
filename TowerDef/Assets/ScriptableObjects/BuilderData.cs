
using UnityEngine;

[CreateAssetMenu(menuName = "Friendly Units/New Builder", fileName = "New Builder unit")]
public class BuilderData : LivingEntityData
{
    public override void Initialize(Entity entity)
    {
        base.Initialize(entity);
    }
}